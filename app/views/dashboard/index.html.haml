%h1= "user = #{current_user.email}"

.region
  %h4= link_to 'Devices', devices_path
  = render partial: 'devices', locals: {devices: @devices}
  .action-links 
    = link_to 'Add Device', new_device_path
